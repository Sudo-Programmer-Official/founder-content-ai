export function logInfo(message: string): void {
  console.info(`[founder-content-api] ${message}`);
}

export function logError(message: string): void {
  console.error(`[founder-content-api] ${message}`);
}
